<UserControl x:Class="KdxDesigner.Controls.ProcessDetailListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:KdxDesigner.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダーとボタン -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBlock Text="ProcessDetail" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="＋ 詳細追加" Click="AddNewProcessDetail_Click"
                    Width="100" Height="25" Margin="0,0,5,0"
                    Background="#2196F3" Foreground="White" FontWeight="Bold"/>
            <Button Content="📋 コピー追加" Click="CopyProcessDetail_Click"
                    Width="110" Height="25" Margin="0,0,5,0"
                    Background="#9C27B0" Foreground="White" FontWeight="Bold"/>
            <Button Content="✕ 削除" Click="DeleteProcessDetail_Click"
                    Width="80" Height="25" Margin="0,0,5,0"
                    Background="#F44336" Foreground="White" FontWeight="Bold"/>
            <Button Content="🔄 再読み込み" Click="ReloadProcessDetails_Click"
                    Width="110" Height="25"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  x:Name="ProcessDetailGrid"
                  Margin="5"
                  ItemsSource="{Binding ProcessDetails, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedProcessDetail, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  SelectionChanged="ProcessDetailGrid_SelectionChanged"
                  PreviewKeyDown="ProcessDetailGrid_PreviewKeyDown"
                  MouseDoubleClick="ProcessDetailGrid_MouseDoubleClick">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="削除" Click="DeleteProcessDetail_Click"/>
                    <MenuItem Header="プロパティを編集" Click="EditProcessDetailProperties_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="40"/>
                <DataGridTextColumn Header="工程ID" Binding="{Binding ProcessId}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="詳細名" Binding="{Binding DetailName}" Width="120"/>
                <DataGridComboBoxColumn Header="操作"
                                        SelectedValueBinding="{Binding OperationId}"
                                        SelectedValuePath="Id"
                                        DisplayMemberPath="OperationName" Width="120">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Operations, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding Operations, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="開始センサ" Binding="{Binding StartSensor}" Width="80"/>
                <DataGridTextColumn Header="終了センサ" Binding="{Binding FinishSensor}" Width="80"/>
                <DataGridComboBoxColumn Header="カテゴリ"
                                        SelectedValueBinding="{Binding CategoryId}"
                                        SelectedValuePath="Id"
                                        DisplayMemberPath="CategoryName" Width="80">
                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding ProcessDetailCategories, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource" Value="{Binding ProcessDetailCategories, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                </DataGridComboBoxColumn>
                <DataGridTextColumn Header="ブロック" Binding="{Binding BlockNumber}" Width="60"/>
                <DataGridTextColumn Header="スキップ" Binding="{Binding SkipMode}" Width="60"/>
                <DataGridTextColumn Header="サイクル" Binding="{Binding CycleId}" Width="60"/>
                <DataGridTextColumn Header="並び順" Binding="{Binding SortNumber}" Width="50"/>
                <DataGridTextColumn Header="コメント" Binding="{Binding Comment}" Width="150"/>
                <DataGridTextColumn Header="IL開始" Binding="{Binding ILStart}" Width="80"/>
                <DataGridTextColumn Header="開始タイマ" Binding="{Binding StartTimerId}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
