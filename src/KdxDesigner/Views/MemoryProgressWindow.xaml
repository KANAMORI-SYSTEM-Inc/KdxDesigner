<Window x:Class="KdxDesigner.Views.MemoryProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="メモリ設定進捗"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- タイトル -->
        <TextBlock Grid.Row="0"
                   Text="メモリ設定処理中..."
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- プログレスバー -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="14"
                       Margin="0,0,0,5"
                       TextWrapping="Wrap"/>
            <ProgressBar Height="25"
                         Minimum="0"
                         Maximum="{Binding ProgressMax}"
                         Value="{Binding ProgressValue}"/>
            <TextBlock HorizontalAlignment="Right"
                       Margin="0,2,0,0"
                       FontSize="12">
                <Run Text="{Binding ProgressValue, Mode=OneWay}"/>
                <Run Text="/"/>
                <Run Text="{Binding ProgressMax, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>

        <!-- ログ出力 -->
        <GroupBox Grid.Row="2" Header="処理ログ" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          x:Name="LogScrollViewer">
                <TextBox Text="{Binding LogMessages, Mode=OneWay}"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         FontFamily="Consolas"
                         FontSize="11"
                         Background="#F5F5F5"
                         BorderThickness="0"
                         VerticalScrollBarVisibility="Auto"
                         TextChanged="LogTextBox_TextChanged"/>
            </ScrollViewer>
        </GroupBox>

        <!-- ボタン -->
        <Button Grid.Row="3"
                Content="閉じる"
                Width="100"
                Height="30"
                HorizontalAlignment="Right"
                Click="CloseButton_Click"
                IsEnabled="{Binding IsCompleted}"/>
    </Grid>
</Window>
