<Window x:Class="KdxDesigner.Views.IOSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="IO選択" Height="600" Width="900" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="インターロックに設定するIOを選択してください"
                   FontSize="14" FontWeight="Bold" Margin="10"/>

        <!-- Search Box -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="検索:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Name="SearchTextBox"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,10,0"/>
            <Button Grid.Column="2" Content="クリア" Width="80"
                    Command="{Binding ClearSearchCommand}"/>
        </Grid>

        <!-- IO List -->
        <DataGrid Grid.Row="2" Name="IODataGrid"
                  ItemsSource="{Binding FilteredIOs}"
                  SelectedItem="{Binding SelectedIO}"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  Margin="10"
                  MouseDoubleClick="IODataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="アドレス" Binding="{Binding Address}" Width="100"/>
                <DataGridTextColumn Header="PLC ID" Binding="{Binding PlcId}" Width="60"/>
                <DataGridTextColumn Header="リンクデバイス" Binding="{Binding LinkDevice}" Width="120"/>
                <DataGridTextColumn Header="IO名" Binding="{Binding IOName}" Width="150"/>
                <DataGridTextColumn Header="説明" Binding="{Binding IOExplanation}" Width="200"/>
                <DataGridTextColumn Header="IOText" Binding="{Binding IOText}" Width="100"/>
                <DataGridTextColumn Header="場所" Binding="{Binding IOSpot}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="選択" Width="80" Height="30" Margin="5" Click="OKButton_Click"/>
            <Button Content="キャンセル" Width="80" Height="30" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>